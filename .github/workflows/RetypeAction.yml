name: RetypeInstallAction 
on:
  push:
defaults:
  run:
    shell: pwsh

jobs:
  Run-Tests:
    runs-on: windows-latest

    steps:
      - uses: actions/checkout@v3
      - name: 🧪 Run PowerShell tests
        run:  |
          gci .\Retype | select -expand fullname
        
        # Invoke-Pester ./tests/Retype.ps1 -Output Detailed -PassThru -ErrorAction Stop
          
