name: RetypeBuildAction
on:
  push:
defaults:
  run:
    shell: pwsh

jobs:
  Run-Dotnet-Build:
    runs-on: windows-latest

    steps:
      - uses: actions/checkout@v3
      - name: 👉 Run PowerShell dotnet build
        run: | 
         sl D:\a\Retype\Retype\Retype
         dotnet build
         dotnet publish
         git clone https://github.com/psDevUK/Retype.git
         Username: psdevuk
         Password: ${{secrets.gitAuth}}
         Set-Location D:\a\Retype\Retype\Retype\bin\Debug\net6.0\
         git config --global user.email "adambacon1@hotmail.com"
         git config --global user.name "psdevuk"
         git init 
         git add publish
         git commit -m "new build"
         git remote add origin git@github.com:psdevuk/Retype.git
         git push -u origin master
         git push origin master
